배열: 연속된 기억공간에 만들어지는 변수의 집합

선언은 자료형[ ] 배열변수명 = new 자료형[갯수];
배열의 시작주소 -> 		new는 연산자이고 
저장					변수를 만들고 만들어진 변수의 시작주소를 반환한다.

참조변수는 주소를 기억하는 변수
for (int i=0; i< num.length; i++){
	num[i] = 50;
	}

for(int a : num){
	sum = sum+a;
	} 배열에 저장된 값을 꺼내서 활용


class : 자료를 속성과 동작 (기능)으로 표현하는방법
--> 사용자 정의 자료형
자료의 설계도
어떻게 구성되어있는지 표현

JVM -(

p.273
Heap : 인스턴스가 생성 = 객체
Method area : 클래스 정보 / 클래스 변수, 클래스 메서드 생성

call stack : 
메서드 내에서 선언된 변수 생성
메서드의 호출 정보

Student student = new Student();인 경우
- student 참조 변수는 메서드 안에 선언된 지역 변수이므로 Call Stack에 저장된다.
new Student()로 생성된 객체는 Heap에 저장된다.

클래스에 기술된 속성이나 메서드가 heap공간에 실제로 만들어짐
인스턴스는 새로 객체를 생성할때를 의미함
 
주소를 기억하는 변수는 참조변수라함

class Student {
	String name; // 인스턴스 변수
	int score;
} 
지역변수와 전역변수가 선언 위치에 따른 변수

인스턴스화 여부에 따른 분류
-인스턴스 변수
- 클래스 변수




메서드 (=함수)
: 반복되는 코드를 집약시켜 여러 번 기술하는 코드의 중복현상을 막기 위해서 사용
- 한 가지 작업을 유도하는 기술

메서드의 종류
- 호출할때 전달되는 자료가 없고 반환되는 값도 없다
- 호출할때 전달되는 자료가 없고 반환되는 값이 있다
- 자료가 있고 반환되는 값은 없다
- 자료가 있고 반환되는 값도 있다

메서드 선언 
[접근제어자] [static] 반환형 메서드형 (매개변수자료형 매개변수명,....) { }

public static void main(String [] args) { }

자료 값도 없고 반환 값도 없으면 
void method01() { }

class Student {
	static int point = 50; 
	// 위 해당 클래스를 이용하여 인스턴스화 한 모든 인스턴스가 공유하는 변수
	// - Method Area에 별도로 생성됨
	// - 인스턴스화 하지 않아도 사용이 가능
	// - 접근방법
		// - 참조변수명.클래스변수명
		// - 클래스명.클래스변수명

	String name;
	int score;
}
